{
  "$description": "Auto-registered by the Figma plugin. Each entry comes from a designer running an audit on that file. Edit `final_pages` here to override LooksLikeScreen detection. The batch CLI (`npm run audit`) walks this same list — keep it sorted alphabetically.",
  "files": []
}
